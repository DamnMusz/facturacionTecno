using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace BITecnored.Controllers
{
    [TestClass]
    public class MotivosDiaPactadoControllerTest
    {
        [TestMethod]
        public void GetTest() // [FromUri] int aseguradora_id, [FromUri] string periodo
        {
            Assert.IsTrue(false);
        }

        private void EjecutarTest() // int aseguradora_id, string periodo
        {
            Assert.IsTrue(false);
        }

        private void ObtenerEstadoTest() // Dictionary<string, MotivoDiaPactado> motivos, string aseguradora_alias, string periodo, int estado
        {
            Assert.IsTrue(false);
        }

        private void ObtenerRealizadasTest() // Dictionary<string, MotivoDiaPactado> motivos, string aseguradora_alias, string periodo
        {
            Assert.IsTrue(false);
        }

        private void ObtenerEnGestionTest() // Dictionary<string, MotivoDiaPactado> motivos, string aseguradora_alias, string periodo
        {
            Assert.IsTrue(false);
        }

        private void ObtenerSinEfectoTest() // Dictionary<string, MotivoDiaPactado> motivos, string aseguradora_alias, string periodo
        {
            Assert.IsTrue(false);
        }
    }
}
