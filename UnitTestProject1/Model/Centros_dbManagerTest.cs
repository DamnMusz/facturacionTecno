using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace BITecnored.Model
{
    [TestClass]
    public class Centros_dbManagerTest
    {
        [TestMethod]
        public void GetAllCentros() // int pagina, int resultados_por_pagina, bool activos)
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void GetCentrosPorBusqueda() // int pagina, int resultados_por_pagina, CentroBusqueda busqueda, bool activos)
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void GetCentrosIncompletos() // int pagina, int resultados_por_pagina)
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void GetCentrosTarifasIncompletas() // int pagina, int resultados_por_pagina)
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void GetCantidadCentros() // CentroBusqueda busqueda, bool activos)
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void GetCantidadCentrosIncompletos()
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void GetCantidadCentrosTarifasIncompletas()
        {
            Assert.IsTrue(false);
        }

        [TestMethod]
        public void Update() // CentroFacturable centro)
        {
            Assert.IsTrue(false);
        }
    }
}